workflows:
  android-workflow:
    name: Android Workflow
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      groups:
        - google_play
      vars:
        PACKAGE_NAME: "com.ultrax.performance"
      node: latest
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build web app
        script: |
          npm run build
      - name: Initialize Capacitor Android
        script: |
          npx cap add android || true
          npx cap sync android
          cd android
          # Regenerate wrapper if it's corrupt or missing
          gradle wrapper
      - name: Build Android package
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease
    artifacts:
      - android/app/build/outputs/apk/release/*.apk
    publishing:
      email:
        recipients:
          - bpahan685@gmail.com
        notify:
          success: true
          failure: true
